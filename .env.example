# Monorepo Bombocado - Exemplo de variáveis de ambiente (raiz)

# --- Backend ---
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bombocado?schema=public"
PORT=3000
CORS_ORIGIN="http://localhost:3001"
JWT_SECRET="change-me-in-production"
BCRYPT_ROUNDS=12
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Frontend (Next.js) ---
# Variáveis expostas no cliente DEVEM começar com NEXT_PUBLIC_
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Como usar:
# 1) Copie este arquivo para .env na raiz.
# 2) Backend já lê automaticamente ../.env (raiz) ou backend/.env.
# 3) Frontend: copie manualmente para frontend/.env.local (ou use o valor da raiz).
#    NEXT não lê .env fora do diretório do app por padrão.


